业务流程
1> 激活功能
    改变数据库字段
2> 发送给用户邮件
3> 点开邮件 (设计url)-----> 访问我们接口
3.1>username ->随机的字符串(用户相关的字段关联 id)--->
3.2> --->  key -value
 redis.set('12345646asdada',1 ,ex=10*60)
 redis.get('12345646asdada')
4>通过id 或者用户名 查询用户,然后修改用户的激活字段


票房排行


热门影院

购票表
id
movie_id 电影
uid 用户表
cinema_id 影院表


选座的功能
电影
影院
影厅
座位表
座位的排期表












